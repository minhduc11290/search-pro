export interface CategoryInfo {
    id: string,
    name: string;
    url: string;
    productUrl: string;
}

export interface CategoryRequest {
    name: string;
    url: string;
    productUrl: string;
}